
1. 一个实现了一致性哈希的模块
   在使用fresh(NumPartitions, SeedNode)确定了ring的分区个数之后, 就不能再修改.
   只能通过update(IndexAsInt, Name, CHash)来修改, 修改某个Index上对应的Node,
   例如:
   我们调用fresh(1024, node0), 会创建1024个'虚拟节点'
   
   Key可以是任意的erlang term, 这些key要经过key_of(ObjectName)处理后, 生成一个160bit的值,
   这个值转换成一个大整数, 可以映射到我们的ring上. 
2.
